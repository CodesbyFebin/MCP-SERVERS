import { describe, it, expect } from "vitest";

// SKIP: requires external CSV data files.
describe.skip("glossary-migration", () => {
  it("placeholder", () => {
    expect(true).toBe(true);
  });
});
